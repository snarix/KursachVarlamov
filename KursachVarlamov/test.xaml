<Window x:Class="KursachVarlamov.test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursachVarlamov"
        mc:Ignorable="d"
        Title="test" Height="450" Width="800">

    <Window.Resources>
        <!-- Определение своего шрифта в ресурсах -->
        <FontFamily x:Key="CustomFontFamily">
            /KursachVarlamov;component/Fonts/#Pixel Times
        </FontFamily>

        <!-- Создание стиля для текста кнопки -->
        <Style x:Key="CustomButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" 
                    Value="{StaticResource CustomFontFamily}" />
            <Setter Property="FontSize" Value="16" />
            <!-- Другие настройки стиля кнопки -->
        </Style>
    </Window.Resources>
    <Grid>
        <!-- Использование стиля для кнопки -->
        <Button Content="Текст кнопки" 
                Style="{StaticResource CustomButtonStyle}" />
    </Grid>

</Window>
